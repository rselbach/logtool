# Example environment for logtool-server
# Copy to /etc/logtool/server.env and adjust values.

# Listen address (override unit ExecStart default)
# LOGTOOL_ADDR=:8080

# Database path (should match StateDirectory path)
LOGTOOL_DB=/var/lib/logtool/monitor.db

# Timezone for bucketing (optional; e.g., -04:00)
LOGTOOL_TZ=+00:00

# Static UI directory (adjust if installed elsewhere)
LOGTOOL_STATIC=/usr/share/logtool/web/dist

# Choose one or more auth methods:

## GitHub OAuth (recommended for interactive login)
GITHUB_CLIENT_ID=your_github_oauth_app_client_id
GITHUB_CLIENT_SECRET=your_github_oauth_app_client_secret
GITHUB_CALLBACK_URL=https://your-domain.com/auth/github/callback

## Apple OAuth (optional alternative for interactive login)
# APPLE_CLIENT_ID=com.yourcompany.logtool.web
# APPLE_TEAM_ID=TEAM123ABC
# APPLE_KEY_ID=KEY123ABC
# APPLE_PRIVATE_KEY_FILE=/etc/logtool/AppleAuthKey_KEY123ABC.p8
# Or inline: APPLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----"
# APPLE_CALLBACK_URL=https://your-domain.com/auth/apple/callback
# APPLE_SCOPES="email name"

## Session configuration (required for GitHub/Apple OAuth)
# Use a long random secret for session cookies (base64 allowed)
LOGTOOL_SESSION_SECRET=REPLACE_WITH_BASE64_SECRET
LOGTOOL_SESSION_TTL=12h
# Enable secure cookies when using HTTPS (nginx terminates TLS)
LOGTOOL_SECURE_COOKIES=true

## Basic auth (optional for API access)
# LOGTOOL_USER=alice
# LOGTOOL_PASS=s3cret

## Bearer tokens (optional for API clients, comma-separated or single)
# LOGTOOL_TOKEN=token123
# LOGTOOL_TOKENS=token1,token2

# IP policy for importer (if you colocate binaries)
# IP_SALT=optional_salt_for_hash_policy
