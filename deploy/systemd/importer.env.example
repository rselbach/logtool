# Example environment for logtool-importer.service
# Copy to /etc/logtool/importer.env and adjust values.

# SQLite database path (should reside under /var/lib/logtool by default)
LOGTOOL_DB=/var/lib/logtool/monitor.db

# Nginx log paths
LOGTOOL_ACCESS=/var/log/nginx/access.log
LOGTOOL_ERROR=/var/log/nginx/error.log

# IP policy for storing remote addresses: store|mask|hash|drop
LOGTOOL_IP_POLICY=mask

# Salt for hash policy (optional)
# IP_SALT=my-long-random-salt

# If the service cannot read /var/log/nginx, you may need to adjust permissions:
# - Make logs group-readable (e.g., root:adm 640) and add SupplementaryGroups=adm to the unit, or
# - Run the unit as User=www-data or User=nginx (commented in the .service), or
# - Grant an ACL: setfacl -m g:logread:r /var/log/nginx/access.log /var/log/nginx/error.log
